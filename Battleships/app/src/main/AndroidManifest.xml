<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.battleships">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"

        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".menu"
                  android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pveMenu" />
        <activity android:name=".deployMenu" />
        <activity android:name=".playerTurn" android:launchMode="singleInstance"/>
        <activity android:name=".aiTurn" android:launchMode="singleInstance"/>
        <activity android:name=".winScreen" />
        <activity android:name=".loseScreen" />
        <activity android:name=".pvpMenu" />
        <activity android:name=".leaderBoard"></activity>
    </application>

</manifest>